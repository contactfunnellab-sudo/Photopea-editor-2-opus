{
  "name": "photopea-feature-swap-executor",
  "version": "0.1.0",
  "description": "Deterministic Photopea-based feature swap executor for the PS Request Fulfiller MVP",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "install-browsers": "npx playwright install chromium"
  },
  "dependencies": {
    "express": "^4.18.0",
    "playwright": "^1.40.0"
  }
}
