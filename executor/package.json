{
  "name": "photopea-primitive-editor",
  "version": "0.2.0",
  "description": "Deterministic Photopea-based primitive editor with MediaPipe face landmarks",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "install-browsers": "npx playwright install chromium"
  },
  "dependencies": {
    "express": "^4.18.0",
    "playwright": "^1.40.0",
    "@tensorflow/tfjs-node": "^4.17.0",
    "@tensorflow-models/face-landmarks-detection": "^1.0.0",
    "sharp": "^0.33.0"
  }
}
